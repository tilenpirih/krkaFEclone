export const useResolutionStore = defineStore('resolution', {
  state: () => ({
    selectedWidth: 0,
    selectedHeight: 0,
    containerWidth: 0,
    scale: 1,
    marginLeft: 0,
  }),
  actions: {
    
  },
})
